"""
The core application of the finances project.

This application contains the main models, views, forms, filters, and templates used
by the project. It is the central application and is responsible for handling the
main pages of the web application.

The models in this application include the Account, Tag, Transaction, and
HistoricBalance models, which are used to store information about the user's
accounts, tags, transactions, and historic balances.

The views in this application include the views for the main pages of the web
application, such as the welcome page, the account list page, the tag list page,
and the transaction list page. The views are responsible for rendering the
templates and providing the data needed for the templates to display.

The forms in this application include the forms for creating and editing accounts,
tags, transactions, and historic balances.

The filters in this application include the filters for filtering the account,
tag, and transaction lists based on the user's input.

The templates in this application include the templates for the welcome page,
account list page, tag list page, and transaction list page.

"""
